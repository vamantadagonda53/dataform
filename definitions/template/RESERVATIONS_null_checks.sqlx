config {
  type: "assertion",
  tags: ["axxerion"],
  schema: "dataform_assertions"
}

SELECT
    status_code,
    status_name
    FROM ${ref('RESERVATIONS')}
WHERE status_code is null or 
    status_name is null 
